# Generated schemas

The TypeScript/Zod definitions under `src/integrations/trainer/schemas/` are the operational source of truth.

The checked-in files here are intentionally present so humans/agents can inspect the protocol immediately. After installing dependencies, run:

```bash
npm run schema:export
```

before treating these JSON Schema files as authoritative generated artifacts.
