import { z } from 'zod';

export const SCHEMA_VERSION = '1.0.0' as const;
export const EntityIdSchema = z.string().min(1).regex(/^[a-z][a-z0-9_:-]*$/);
export const IsoDateTimeSchema = z.iso.datetime({ offset: true });
export const IsoDateSchema = z.iso.date();
export const EquipmentSchema = z.object({id:EntityIdSchema,name:z.string().min(1),category:z.enum(['machine','barbell','dumbbell','bench','cable','bodyweight','band','other']),notes:z.string().optional()}).strict();
export const EnvironmentSchema = z.object({id:EntityIdSchema,name:z.string().min(1),equipmentIds:z.array(EntityIdSchema),notes:z.string().optional()}).strict();
export const ExerciseMediaSchema = z.object({id:EntityIdSchema,type:z.enum(['youtube','video_url','image_url']),url:z.url(),label:z.string().optional()}).strict();
export const ExerciseSchema = z.object({id:EntityIdSchema,name:z.string().min(1),aliases:z.array(z.string()).default([]),movementPattern:z.string().optional(),primaryMuscles:z.array(z.string()),secondaryMuscles:z.array(z.string()),requiredEquipmentIds:z.array(EntityIdSchema),optionalEquipmentIds:z.array(EntityIdSchema).default([]),instructions:z.string().optional(),executionNotes:z.array(z.string()).default([]),media:z.array(ExerciseMediaSchema).default([]),substitutionExerciseIds:z.array(EntityIdSchema).default([]),isUnilateral:z.boolean().default(false)}).strict();
export const ResistanceSchema = z.discriminatedUnion('type',[z.object({type:z.literal('weight'),value:z.number().nonnegative(),unit:z.enum(['kg','lb'])}).strict(),z.object({type:z.literal('bodyweight'),additionalWeightKg:z.number().nonnegative().optional()}).strict(),z.object({type:z.literal('assisted_bodyweight'),assistanceKg:z.number().nonnegative()}).strict(),z.object({type:z.literal('machine_level'),level:z.number().nonnegative()}).strict(),z.object({type:z.literal('band'),label:z.string().optional()}).strict()]);
export const RepRangeSchema = z.object({min:z.number().int().positive(),max:z.number().int().positive()}).refine(v=>v.min<=v.max,'min must be <= max');
export const RirRangeSchema = z.object({min:z.number().int().min(0).max(10),max:z.number().int().min(0).max(10)}).refine(v=>v.min<=v.max,'min must be <= max');
export const PrescriptionSubstitutionSchema = z.object({exerciseId:EntityIdSchema,override:z.object({sets:z.number().int().positive().optional(),reps:RepRangeSchema.optional(),targetRir:RirRangeSchema.optional(),restSeconds:z.number().int().nonnegative().optional()}).strict().optional()}).strict();
export const ExercisePrescriptionSchema = z.object({id:EntityIdSchema,exerciseId:EntityIdSchema,sets:z.number().int().positive(),reps:RepRangeSchema,targetRir:RirRangeSchema.optional(),restSeconds:z.number().int().nonnegative(),notes:z.array(z.string()).default([]),substitutions:z.array(PrescriptionSubstitutionSchema).default([])}).strict();
export const WorkoutVariantSchema = z.object({id:EntityIdSchema,environmentId:EntityIdSchema,targetDurationMinutes:z.number().int().positive(),prescriptionIds:z.array(EntityIdSchema).min(1)}).strict();
export const WorkoutSchema = z.object({id:EntityIdSchema,name:z.string().min(1),prescriptions:z.array(ExercisePrescriptionSchema),variants:z.array(WorkoutVariantSchema)}).strict();
export const TrainingPlanSchema = z.object({id:EntityIdSchema,name:z.string().min(1),validFrom:IsoDateSchema,validUntil:IsoDateSchema.optional(),workouts:z.array(WorkoutSchema).min(1),notes:z.array(z.string()).default([])}).strict();
export const SetLogSchema = z.object({number:z.number().int().positive(),type:z.enum(['warmup','working','drop','backoff']).default('working'),resistance:ResistanceSchema.optional(),reps:z.number().int().nonnegative().optional(),rir:z.number().int().min(0).max(10).optional(),restAfterSeconds:z.number().int().nonnegative().optional(),completedAt:IsoDateTimeSchema.optional(),notes:z.string().optional()}).strict();
export const ExerciseSessionSchema = z.object({id:EntityIdSchema,exerciseId:EntityIdSchema,prescriptionId:EntityIdSchema.optional(),order:z.number().int().nonnegative(),substitution:z.object({originalExerciseId:EntityIdSchema,reason:z.enum(['equipment_unavailable','equipment_busy','discomfort','preference','other'])}).strict().optional(),sets:z.array(SetLogSchema),notes:z.string().optional()}).strict();
export const TrainingSessionSchema = z.object({id:EntityIdSchema,planId:EntityIdSchema.optional(),workoutId:EntityIdSchema,variantId:EntityIdSchema.optional(),environmentId:EntityIdSchema,startedAt:IsoDateTimeSchema,endedAt:IsoDateTimeSchema.optional(),status:z.enum(['in_progress','completed','abandoned']),exercises:z.array(ExerciseSessionSchema),notes:z.string().optional()}).strict();
export const TrainingHistorySchema = z.object({from:IsoDateSchema,to:IsoDateSchema,sessions:z.array(TrainingSessionSchema)}).strict();
export const TrainingProfileSchema = z.object({goals:z.array(z.string()),experienceLevel:z.enum(['beginner','intermediate','advanced']).optional(),preferredUnits:z.enum(['metric','imperial']).default('metric'),constraints:z.array(z.object({id:EntityIdSchema,type:z.enum(['equipment','movement','exercise','schedule','other']),description:z.string().min(1)}).strict()).default([]),notes:z.string().optional()}).strict();
